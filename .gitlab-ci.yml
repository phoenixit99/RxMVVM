stages:
  - build
  - test
  - release
  - deploy
  
variables:
  LC_ALL: "en_US.UTF-8"
  LANG: "en_US.UTF-8"

build_project:
  stage: build
  script:
    # - sudo gem install bundler
    # - bundle install
    - bundle exec fastlane telegr
  tags:
    - master

# build_project:
#   stage: build
#   script:
#     - sudo gem install bundler
#     - bundle install
#     - bundle exec fastlane build
#   tags:
#     - master

# release:
#   stage: release
#   script:
#   - pod install
#   - bundle exec fastlane release
#   artifacts:
#     paths:
#       - build/RxMVVM.ipa
#   tags:
#     - stable
#   only:
#      - /^release-.*$/
#      - develop
# deploy:
#   stage: deploy
#   script:
#   - bundle exec fastlane deploy
#   tags:
#     - master
